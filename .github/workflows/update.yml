name: update_data
on:
  push:
    branches:
    paths:
      - .github/workflows/update.yml
      - .github/workflows/build.yml
  schedule:
    - cron: 0 * * * *
  workflow_dispatch:

env:
  IS_REPARSE: ${{ github.event_name == 'workflow_dispatch' }}
  IS_RELEASE_BRANCH: ${{ github.ref_name == 'stable' }}

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      use_release: ${{ env.IS_RELEASE_BRANCH }}

  update:
    name: update
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Run (schedule)
        if: ${{ !env.IS_REPARSE }}
        run: ./PoliNetwork.Graduatorie.Parser/bin/Release/net7.0/PoliNetwork.Graduatorie.Parser

        ### REPARSE (push, manual trigger)
      - name: Run (reparse)
        if: ${{ env.IS_REPARSE }}
        run: ./PoliNetwork.Graduatorie.Parser/bin/Release/net7.0/PoliNetwork.Graduatorie.Parser --reparse

      - name: Push generated files
        uses: EndBug/add-and-commit@v9 # You can change this to use a specific version.
        with:
          cwd: "./local/"
          add: "data"
          message: ${{ env.IS_REPARSE && '[auto] update data with reparse' || '[auto] update data' }}
          author_name: PoliNetwork
          author_email: PoliNetwork@users.noreply.github.com
          pathspec_error_handling: exitImmediately
